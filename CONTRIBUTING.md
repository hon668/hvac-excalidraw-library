# 贡献指南 CONTRIBUTING

感谢你愿意往这个库添砖加瓦 🙌
**这个库最大的价值就是"元件够全"**，所以每一个新元件、每一处图形修正都有意义。

---

## 一、我需要会写代码吗？

**不需要。** 只要你会用 Excalidraw 画图 + 会用 Git 提交，就能贡献。有两条路线：

| 路线 | 适合谁 | 工作量 | 推荐度 |
| --- | --- | --- | --- |
| **路线 A：改脚本** | 会一点 Python | 写 10~30 行代码 | ⭐⭐⭐ 推荐，可复现、易评审 |
| **路线 B：手绘导出** | 只会 Excalidraw | 画完导出即可 | ⭐⭐ 也完全可以 |

两条路线产出的都是 `.excalidrawlib`，效果一致。选你顺手的。

---

## 二、元件设计规范（务必遵守，否则 PR 会被要求返工）

这一套规范是为了**让 16 个元件混在一张图里不违和**。请对照下表逐项检查。

### 2.1 线条与配色

| 项目 | 规范值 | 说明 |
| --- | --- | --- |
| 主描边色 | `#1e1e1e` | 所有设备轮廓、结构线 |
| 主描边宽度 | `2` | 手绘粗度基准 |
| 辅助构造线色 | `#868e96` | 内部构造（叶片、滤网、隔板），宽度 `1` ~ `1.5` |
| 水管 / 冷媒色 | `#1971c2` | 蓝 |
| 风管 / 气流色 | `#2f9e44` | 绿 |
| 热水 / 蒸汽色 | `#e8590c` | 橙 |
| 冷却水色 | `#e8590c` | 橙（与热水共用一色，同一张图里不要同时出现两种含义） |
| 浅填充色 | `#e9ecef` | 设备外壳填充，配合 `fillStyle = solid` |
| 其它填充 | `transparent` | 不要用彩色填充，会喧宾夺主 |
| 粗糙度 roughness | `1` | 保持手绘感，别用 `0`（会变成 CAD 味） |
| 线型 strokeStyle | `solid` | 虚线只用于"二次侧 / 回水 / 备用 / 逻辑连接"语义 |

### 2.2 几何与尺寸

| 项目 | 规范 |
| --- | --- |
| 参考网格 | 1 格 ≈ 20 px；主要设备宽度取 **8~13 格（160~260 px）** |
| 坐标原点 | **元件左上角对齐 `(0, 0)`**，不要带偏移（避免插入画布后飞到很远） |
| 阀件/仪表类 | 整体控制在 **90 px 以内**，与设备按真实比例关系协调 |
| 圆角 | 箱体类可用圆角，管道/风管类保持直角 |
| 自带标签 | 放在元件**正下方居中**，字号 `14`~`16`，颜色 `#1e1e1e` 或 `#868e96` |
| 接管端头 | 必须留在元件**体外**（伸出轮廓 20~40 px），方便下游管线接驳 |
| ⚠️ 接管与标签 | **接管端头不要放在自带标签的正下方/正上方**——否则下游管线接上去必然穿过标签文字。水管类元件优先让接管从**左右两侧**伸出 |

### 2.3 分组与命名

- ✅ **必须分组**：同一元件的所有元素选中后 <kbd>Ctrl/Cmd</kbd> + <kbd>G</kbd> 成组，插入画布时才能整体拖动
- ✅ **命名规范**：
  - 中文库：`设备名 英文缩写`，例：`冷水机组`、`空气处理机组 AHU`、`风机盘管 FCU`
  - 英文库：用行业通用英文术语，例：`Chiller`、`Air Handling Unit (AHU)`、`Fan Coil Unit (FCU)`
- ❌ 不要包含项目名、公司名、水印、Logo
- ❌ 不要把多个不相关设备塞进同一个元件

### 2.4 字体

字体统一用 Excalidraw 的手写体（`Excalifont / Virgil`，即 `fontFamily = 1`）。
中文在没有手写体中文字体时会自动回退到系统黑体，这是正常现象，不用改。

---

## 三、路线 A：改脚本新增元件（推荐）

```bash
git clone https://github.com/hon668/hvac-excalidraw-library.git
cd hvac-excalidraw-library
python tools/build_library.py   # 先跑通，确认环境 OK
```

1. 打开 `tools/build_library.py`
2. 仿照现有的 `c_filter(zh)`（最简单的那个）写一个新函数，例如 `c_cooling_tower(zh)`
3. 把新函数注册进 `COMPONENTS` 列表 —— 元组的顺序就是最终库里的元件顺序：

```python
COMPONENTS = [
    ("冷水机组",        "Chiller",                 c_chiller),
    ...
    ("冷却塔",          "Cooling Tower",           c_cooling_tower),   # ← 加在这里
]
```

4. 重新运行 `python tools/build_library.py`，会自动重出：
   - `library/*.excalidrawlib`（中英双库）
   - `examples/*.excalidraw`
   - `docs/assets/preview-*`（含 PNG）
5. 打开 `docs/assets/preview-components.png` **肉眼确认**新元件画得对不对（这一步别省）
6. 更新 `README.md` 的元件清单表格和数量徽章、更新 `CHANGELOG.md`
7. 提交 PR

> 脚本里的坐标辅助函数：`rect()` 矩形、`ellipse()` 椭圆、`polyline()` 折线、`arrow()` 箭头、`label()` 居中文字，
> 配色常量在文件顶部。所有坐标都是绝对坐标，不用自己算 bounding box。

---

## 四、路线 B：Excalidraw 手绘导出

1. Fork 并 clone 仓库（同上）
2. 打开 <https://excalidraw.com>
3. **先装载现有库**：库面板 → Browse libraries → Open → 选 `library/hvac-excalidraw-library-zh-v1.0.0.excalidrawlib`
   （这样新元件才能和旧元件一起导出）
4. 在画布空白处按第二节规范画新元件，选中全部元素 → <kbd>Ctrl/Cmd</kbd>+<kbd>G</kbd> 成组
5. 选中新元件 → 库面板里点「+ / 添加到库」把它加进库
6. 在库面板里把该元件**改名为规范名**（右键 / 悬停改名）
7. 库面板 → **Export / 导出为库文件** → 覆盖 `library/hvac-excalidraw-library-zh-vX.Y.Z.excalidrawlib`
   （文件名里的版本号记得按 [VERSIONING.md](VERSIONING.md) 升位，例如 `v1.0.0` → `v1.1.0`，旧文件删除）
8. 顺手把 `README.md` 元件清单、`CHANGELOG.md` 一并更新
9. 提交 PR

> 英文库（`-en-` 那个）可以由你一并提交，也可以在 PR 里说明"英文库待维护者补齐"，维护者会帮忙。

---

## 五、提交 PR 的规范

### 5.1 分支命名

```
feat/add-cooling-tower        # 新增元件
fix/chiller-pipe-direction    # 修正图形
docs/obsidian-import-guide    # 文档
chore/bump-v1.1.0             # 版本号/杂务
```

### 5.2 提交信息（Conventional Commits）

```
feat(lib): add cooling tower component
fix(lib): correct pump outlet direction
docs: add Obsidian import screenshots
chore: bump version to v1.1.0
```

### 5.3 PR 描述

PR 模板会自动加载，请把这几项填完：

- **改了什么**：一句话说清
- **影响哪个库文件**：中文库 / 英文库 / 两个都改了
- **附截图**：新元件在画布上的效果截图（**必须**，评审主要看图）
- **自检清单**：逐项打勾

### 5.4 评审标准

维护者会按这几点看你的 PR：

| 检查项 | 通过标准 |
| --- | --- |
| 是否符合配色/粗度规范 | 与现有 16 个元件放在一张图里不违和 |
| 是否成组 | 插入画布后能整体拖动 |
| 坐标是否归零 | 拖到画布时不会跑到很远的地方 |
| 命名是否规范 | 中英命名符合 2.3 节 |
| 是否可复用 | 不能被一眼看出是"为某一张图临时画的" |
| 预览图是否更新 | `docs/assets/` 下的预览已重新生成 |
| 文档是否同步 | README 清单 + CHANGELOG 已更新 |

维护者可能会提修改意见，改完在同一个 PR 里继续提交即可，**不要另开 PR**。

---

## 六、提 Issue 的规范

### 6.1 想要新元件 → 用「新元件需求」模板

请务必写清：

| 字段 | 说明 | 例子 |
| --- | --- | --- |
| 元件名称 | 中文名 + 英文名 | 冷却塔 / Cooling Tower |
| 使用场景 | 你会用它画什么图 | 冷却水系统原理图、屋面设备布置 |
| 简化程度 | 图形能简化到什么程度 | 只需方形塔体 + 风机 + 进出水管，不用画填料 |
| 参考图 | 手绘草图 / 照片 / 现有图集链接 | 贴图或链接 |
| 是否需要文字标签 | 默认带 | 要 / 不要 |

> 提需求时如果能顺手画个手绘草图（纸笔拍照就行），实现速度会快很多。

### 6.2 元件画错了 → 用「问题反馈」模板

**必须写清**：

- 出问题的元件名
- 具体哪里不对（出口方向反了 / 线没接上 / 文字重叠 / 插入后位置跑偏）
- 截图（带标注圈出问题处最好）
- 你的 Excalidraw 使用环境（网页版 or Obsidian 插件 + 版本号）

### 6.3 Issue 标题

```
[新元件] 冷却塔 Cooling Tower
[Bug] 循环水泵出口方向与规范相反
[建议] 希望增加英文版的 AHU 分段命名
```

---

## 七、不要提交什么

- ❌ 与暖通/机电无关的元件（装饰图案、卡通头像等）
- ❌ 从别的素材库复制来、只改了个颜色的元件（官方库和我们都不接受）
- ❌ 单个箭头、单个方块这类"随手就能画"的图形
- ❌ 带版权素材、厂商 Logo、真实项目图纸信息的元件
- ❌ 体积巨大的图片型元件（本库坚持纯矢量）

---

## 八、许可

**提交 PR 即表示你同意你的贡献以仓库的 [MIT License](LICENSE) 授权发布**，允许任何人自由使用、修改、再分发。
请确保你提交的元件是你原创绘制的。

有任何疑问，直接在 Issue 里问，不用客气。
